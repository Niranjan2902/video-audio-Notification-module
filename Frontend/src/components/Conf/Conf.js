const Conf={
    storage_password:String(import.meta.env.storage_key),
    zone:String(import.meta.env.zone),
    libraryId:String(import.meta.env.libraryId),
    stream_key:String(import.meta.env.stream_api_key),
    vidId:String(import.meta.env.vidId),
}
export default Conf